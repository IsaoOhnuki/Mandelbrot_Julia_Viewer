<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Mandelbrot_Julia_Viewer.Views.DrawPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand" BackgroundColor="Chocolate">
            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" BackgroundColor="AliceBlue">
                <Label Text="拡大" VerticalOptions="Center"/>
                <Slider x:Name="Slider1" Value="{Binding Rate}" HorizontalOptions="FillAndExpand" Maximum="5" Minimum="1"/>
            </StackLayout>
            <Image x:Name="ImageControl" Source="{Binding ImageSource}" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"/>
            <ActivityIndicator BindingContext="{x:Reference ImageControl}" IsRunning="{Binding IsLoading}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>